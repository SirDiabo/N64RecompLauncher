<Window x:Class="N64RecompLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:N64RecompLauncher"
        Title="N64Recomp Launcher" 
        Height="720" Width="1280"
        Background="#1e1e1e"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:BooleanToStretchConverter x:Key="BooleanToStretchConverter"/>
        <local:ThicknessConverter x:Key="ThicknessConverter"/>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{Binding ButtonColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OptionsButton" TargetType="Button">
            <Setter Property="Background" Value="#3a3a3a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4a4a4a"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2a2a2a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsButton" TargetType="Button">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#404040"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1a1a1a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GameCard" TargetType="Border">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="ContextMenuStyle" TargetType="ContextMenu">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="BorderBrush" Value="#4a4a4a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
        </Style>

        <Style x:Key="ContextMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border Background="{TemplateBinding Background}" 
                        Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#4a4a4a"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="False">
                                <Setter Property="Background" Value="#2a2a2a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Storyboard x:Key="ExpandSettingsPanel">
            <DoubleAnimation Storyboard.TargetName="SettingsPanel"
                             Storyboard.TargetProperty="Width"
                             To="284"
                             Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="SettingsPanel"
                             Storyboard.TargetProperty="Opacity"
                             To="1"
                             Duration="0:0:0.2"/>
        </Storyboard>

        <Storyboard x:Key="CollapseSettingsPanel">
            <DoubleAnimation Storyboard.TargetName="SettingsPanel"
                             Storyboard.TargetProperty="Width"
                             To="0"
                             Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="SettingsPanel"
                             Storyboard.TargetProperty="Opacity"
                             To="0"
                             Duration="0:0:0.2"/>
        </Storyboard>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition x:Name="SettingsPanelColumn" Width="0"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <local:AnimatedGifImage GifSource="/Assets/icon.gif"
                                Opacity="0.2"
                                Width="Auto"
                                Height="512"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Grid.RowSpan="2"
                                Grid.ColumnSpan="3"
                                Panel.ZIndex="-1"/>

        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,20">
            <TextBlock Text="N64Recomp Launcher"
                       FontSize="32" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,15,0" VerticalAlignment="Top">
            <Button Style="{StaticResource SettingsButton}"
                    Background="Transparent"
                    Width="60"
                    Height="60"
                    Click="SettingsButton_Click"
                    ToolTip="Settings">
                <Image Source="/Assets/Settings.png" Width="60" Height="60" Margin="-16"/>
            </Button>
        </StackPanel>

        

        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,15,0" VerticalAlignment="Bottom">
            <Button Style="{StaticResource SettingsButton}"
                    Background="Transparent"
                    Width="60"
                    Height="60"
                    Click="GithubButton_Click"
                    ToolTip="Open N64RecompLauncher Github Page">
                <Image Source="/Assets/Github.png" Width="60" Height="60" Margin="-16"/>
            </Button>
            <Button Style="{StaticResource SettingsButton}"
                    Background="Transparent"
                    Width="60"
                    Height="60"
                    Click="DiscordButton_Click"
                    ToolTip="Join N64 Recomp Discord Server">
            <Image Source="/Assets/Discord.png" Width="60" Height="60" Margin="-16"/>
            </Button>
        </StackPanel>

            <Border x:Name="SettingsPanel"
                Grid.Row="1" 
                Grid.Column="1"
                Width="0"
                HorizontalAlignment="Left"
                Background="#2a2a2a"
                CornerRadius="8"
                Margin="0,0,0,0"
                Opacity="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="20" x:Name="SettingsContent">
                    <TextBlock Text="Settings" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,0,0,0"/>

                    <TextBlock Text="General" 
                               FontSize="16" 
                               FontWeight="SemiBold" 
                               Foreground="#cccccc" 
                               Margin="0,0,0,10"/>

                    <CheckBox x:Name="PortableCheckBox"
                              Content="Portable" 
                              Foreground="White" 
                              Margin="0,0,0,0"
                              Checked="PortableCheckBox_Checked"
                              Unchecked="PortableCheckBox_Unchecked" />

                    <TextBlock Text="Store data in the game directory."
                               FontSize="12" 
                               Foreground="#999999" 
                               Margin="0,0,0,10"/>

                    <TextBlock Text="UI" 
                               FontSize="16" 
                               FontWeight="SemiBold" 
                               Foreground="#cccccc" 
                               Margin="0,0,0,10"/>

                    <CheckBox x:Name="IconFillCheckBox"
                               Content="Image Fill" 
                               Foreground="White" 
                               Margin="0,0,0,0"
                               Checked="IconFillCheckBox_Checked"
                               Unchecked="IconFillCheckBox_Unchecked" />

                    <TextBlock Text="Images fill the entire slots."
                               FontSize="12" 
                               Foreground="#999999" 
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Image Opacity" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="White"/>
                    <TextBlock Text="{Binding ElementName=IconOpacitySlider, Path=Value}" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="White"/>

                    <Slider x:Name="IconOpacitySlider"
                               Foreground="White" 
                               Margin="0,0,0,0"
                               Minimum="0"
                               Maximum="1"
                               Value="0.2"
                               TickFrequency="0.1"
                               IsSnapToTickEnabled="True"
                               ValueChanged="IconOpacitySlider_ValueChanged" />

                    <TextBlock Text="Opacity of the game images."
                               FontSize="12"
                               Foreground="#999999" 
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Image Size" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="White"/>
                    <TextBlock Text="{Binding ElementName=IconSizeSlider, Path=Value}" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="White"/>

                    <Slider x:Name="IconSizeSlider"
                               Foreground="White" 
                               Margin="0,0,0,0"
                               Minimum="64"
                               Maximum="512"
                               Value="256"
                               TickFrequency="8"
                               IsSnapToTickEnabled="True"
                               ValueChanged="IconSizeSlider_ValueChanged" />

                    <TextBlock Text="Size of the game images."
                               FontSize="12" 
                               Foreground="#999999" 
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Image Margin" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="White"/>
                    <TextBlock Text="{Binding ElementName=IconMarginSlider, Path=Value}" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="White"/>

                    <Slider x:Name="IconMarginSlider"
                               Foreground="White" 
                               Margin="0,0,0,0"
                               Minimum="0"
                               Maximum="512"
                               Value="0"
                               TickFrequency="8"
                               IsSnapToTickEnabled="True"
                               ValueChanged="IconMarginSlider_ValueChanged" />

                    <TextBlock Text="The image's left margin."
                               FontSize="12" 
                               Foreground="#999999" 
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Game Text Margin" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="White"/>
                    <TextBlock Text="{Binding ElementName=TextMarginSlider, Path=Value}" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="White"/>

                    <Slider x:Name="TextMarginSlider"
                               Foreground="White" 
                               Margin="0,0,0,0"
                               Minimum="0"
                               Maximum="256"
                               Value="0"
                               TickFrequency="8"
                               IsSnapToTickEnabled="True"
                               ValueChanged="TextMarginSlider_ValueChanged" />

                    <TextBlock Text="Horizontal spacing of texts inside game slots."
                               FontSize="12" 
                               Foreground="#999999" 
                               Margin="0,0,0,10"/>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <ScrollViewer Grid.Row="1" Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Games}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource GameCard}" Height="120">
                            <Grid>
                                <Image Source="{Binding IconUrl}" 
                                    Height="{Binding ElementName=IconSizeSlider, Path=Value}"
                                    VerticalAlignment="Center" 
                                    HorizontalAlignment="Left"
                                    Opacity="{Binding ElementName=IconOpacitySlider, Path=Value}" 
                                    Margin="{Binding ElementName=IconMarginSlider, Path=Value, Converter={StaticResource ThicknessConverter}}"
                                    Stretch="{Binding Path=IsChecked, ElementName=IconFillCheckBox, Converter={StaticResource BooleanToStretchConverter}}" />
                                    <Grid Margin="20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Row="1" Margin="{Binding ElementName=TextMarginSlider, Path=Value, Converter={StaticResource ThicknessConverter}}">

                                        <TextBlock Text="{Binding Name}" 
                                                       FontSize="20" 
                                                       FontWeight="SemiBold" 
                                                       Foreground="White"/>
                                        <TextBlock Text="{Binding StatusText}" 
                                                       FontSize="14" 
                                                       Foreground="#cccccc" 
                                                       Margin="0,5,0,0"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                            <TextBlock Text="Latest: " 
                                                           FontSize="12" 
                                                           Foreground="#999999"/>
                                            <TextBlock Text="{Binding LatestVersion}" 
                                                           FontSize="12" 
                                                           Foreground="#cccccc"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Vertical">
                                        <Button Style="{StaticResource ModernButton}"
                                                Content="{Binding ButtonText}"
                                                MinWidth="120"
                                                Margin="0,0,0,10"
                                                IsEnabled="{Binding IsLoading, Converter={x:Static local:BoolInverterConverter.Instance}}"
                                                Click="GameButton_Click"
                                                Tag="{Binding}"/>

                                        <Button Style="{StaticResource OptionsButton}"
                                                Content="Options"
                                                Padding="2"
                                                Width="120"
                                                Height="30"
                                                FontSize="12"
                                                IsEnabled="{Binding IsLoading, Converter={x:Static local:BoolInverterConverter.Instance}}"
                                                Click="OptionsButton_Click"
                                                Tag="{Binding}">
                                            <Button.ContextMenu>
                                                <ContextMenu Style="{StaticResource ContextMenuStyle}"
                                                             Placement="Bottom"
                                                             PlacementTarget="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Button}}">
                                                    <MenuItem Header="Open GitHub Page"
                                                              Style="{StaticResource ContextMenuItemStyle}"
                                                              Click="OpenGitHubPage_Click"
                                                              Tag="{Binding}"/>
                                                    <MenuItem Header="Add Custom Image"
                                                              Style="{StaticResource ContextMenuItemStyle}"
                                                              Click="SetCustomIcon_Click"
                                                              Tag="{Binding}"/>
                                                    <MenuItem Header="Remove Custom Image"
                                                              Style="{StaticResource ContextMenuItemStyle}"
                                                              Click="RemoveCustomIcon_Click"
                                                              Tag="{Binding}"
                                                              Visibility="{Binding HasCustomIcon, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                    <MenuItem Header="Delete"
                                                              Style="{StaticResource ContextMenuItemStyle}"
                                                              Click="DeleteGame_Click"
                                                              Tag="{Binding}"/>
                                                </ContextMenu>
                                            </Button.ContextMenu>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Text="{Binding currentVersionString}"
                   Grid.Row="1"
                   Grid.Column="2"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Left"
                   Margin="0,0,0,0"
                   Foreground="#808080"
                   FontSize="12" />
    </Grid>
</Window>